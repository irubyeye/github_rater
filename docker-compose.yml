services:
  github-rater:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - github-rater-data:/app/data
    restart: unless-stopped

volumes:
  github-rater-data:
